<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Data</title>
    <link rel="stylesheet" href="/edit.css">
</head>
<body>
    <div class="container">
        <h1>Edit your chat</h1>
         <h3>This chat was sent From <%= newchat.from %> to <%= newchat.to %></h3>
        <form action="/chats/<%= newchat._id %>?_method=PUT" method="POST">
            <div class="form-group">
                <label for="name">From</label>
                <input type="text" id="name" name="name" value="<%= newchat.from %>" required>
            </div>
            <div class="form-group">
                <label for="bio">Message</label>
                <textarea id="bio" name="msg" required><%= newchat.msg %></textarea>
            </div>
            <div class="form-group">
                <label for="name">Sent</label>
                <input type="text" id="name" name="name" value="<%= newchat.to%>" required>
            </div>
            <div class="form-group">
                <button type="submit">Save Changes</button>
            </div>
        </form>
    </div>
</body>
</html>
